# any way to take a file as a parameter and plot that?
# would still need to change the title though
set view map
unset key
set lmargin at screen 0.1
set rmargin at screen 0.85
set title "Static structure factor for charge-charge correlations using Maggs-Rossetto\nk_x = {/Symbol p} plane"
splot "s_ch_kxpi.out" w image

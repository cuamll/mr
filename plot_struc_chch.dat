# any way to take a file as a parameter and plot that?
# would still need to change the title though
set view map
unset key
set lmargin at screen 0.1
set rmargin at screen 0.85
set title "Static structure factor for charge-charge correlations - Maggs-Rossetto"
dx=70
#splot "<awk '{ if(NR > 196 && NR < 246) print $2,$3,$4 }' s_q.out" w image
plot "<awk '{ print $1,$2,$3 }' struc_e_k0.out | sort" w image pixels
